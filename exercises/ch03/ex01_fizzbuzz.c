/*
 * Exercise 3.1: FizzBuzz
 *
 * The classic interview problem:
 * - Print numbers 1 to 100
 * - If divisible by 3, print "Fizz" instead
 * - If divisible by 5, print "Buzz" instead
 * - If divisible by both 3 and 5, print "FizzBuzz" instead
 *
 * Python version:
 *   for i in range(1, 101):
 *       if i % 15 == 0:
 *           print("FizzBuzz")
 *       elif i % 3 == 0:
 *           print("Fizz")
 *       elif i % 5 == 0:
 *           print("Buzz")
 *       else:
 *           print(i)
 *
 * Compile: cc -Wall -o ex01_fizzbuzz ex01_fizzbuzz.c
 * Run: ./ex01_fizzbuzz
 */

#include <stdio.h>

int main(void) {
    // TODO: Implement FizzBuzz
    // Hint: Use a for loop: for (int i = 1; i <= 100; i++)
    // Hint: Use if/else if/else with % operator

    return 0;
}
